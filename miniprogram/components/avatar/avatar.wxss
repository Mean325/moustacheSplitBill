/* components/avatar/avatar.wxss */
.avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  text-align: center;
  font-size: 22px;
  background: var(--THEME);
  color: #FFFFFF;
  overflow: hidden;
}